/*
.*

# INCLUDE BACK
# ------------

# DIRECTORIES
# ------------

!src
!public

# include so that generated source code zips using wxt have it
# makes the source code zip usable for submission, even though the docker
# stuff isn't needed for docker itself.
!docker

# DIRECTORIES
# ============

# FILES
# ------------

!manifest.json
!package.json
!pnpm-lock.yaml
!pnpm-workspace.yaml


!tsconfig.json
!wxt.config.ts
!eslint.config.js

!_config.example.ts
!build.sh

!README.md

# FILES
# ============

# INCLUDE BACK
# ============